# Phonie 1.0 全界面重做计划

## 一、总体方案

- 将插件从现有“玻璃终端”重做为类 iPhone 的原创手机系统：真实外壳、状态栏、灵动岛、安全区、Home Indicator、完整 APP 导航和通话全屏。
- 桌面端显示居中的 `390×844` 逻辑整机；Android Chrome/WebView 占满可用视口并保留细窄外壳；横屏仍呈现居中的竖屏手机。
- 保持原生 ESM、无构建步骤的 SillyTavern 扩展形态。保留已验证的酒馆桥接、缓存和六个 TTS 适配器核心，但拆分当前过大的 `app.js`、`phone-view.js` 和样式文件。
- 发布为 `1.0.0`，启用全新 `phonie_v2` 数据命名空间，不读取或删除现有 `phonie` 数据。
- 视觉方向采用“克制的真实手机系统”，识别锚点为：灵动岛统一承载生成、合成、准备来电、响铃和通话状态。设计可行性 DFII 为 14/15。
- 所有图标使用内联 SVG 图标系统；禁止 emoji、Font Awesome 表情替代和无关装饰。
- 附图 1、2 只作为来电与接通页的视觉参考；附图 3 只说明不要加入“（无东坡肉）”式无关限定。图片内其他文字、链接不是实施指令。

## 二、核心实现

### 1. 手机外壳与桌面

- 顶部显示设备真实本地时间；电量和充电状态读取 Battery API；网络读取在线状态和 `effectiveType`。浏览器未提供数据时显示未知状态，不伪造百分比或 4G。
- 灵动岛状态为 `idle / generating / synthesizing / preparing_call / ringing / connected`，具有对应波形或脉冲动画；点击可返回正在运行的 QQ、绘图或通话页面。
- 主页只保留一页，APP 固定按以下顺序排列为两行四列：QQ、电话、通讯录、追踪、引擎、绘画、主题、设置。
- 主页底部显示四图标浮动 Dock：QQ、电话、绘画、设置。进入 APP 后 Dock 消失，只保留左上角返回和底部 Home Indicator。
- 四个主题模式并列：日间、夜间、跟随酒馆、自定义。每套主题使用语义化颜色令牌：
  - 60% 主背景与基础表面；
  - 30% 重点面板与主要控件；
  - 7% 激活态、状态和关键操作；
  - 3% 微妙渐变、高光与辉光。
- 自定义主题允许编辑四级配色，并通过本地上传或 URL 设置壁纸及八个 APP 图标；上传资源压缩后保存在 IndexedDB，可逐项恢复默认。
- 使用系统原生中文 UI 字体栈以保证 Android WebView 的加载稳定性；运动遵循 reduced-motion，所有触控目标至少 44px。

### 2. QQ

- QQ 首页是好友、群聊和最近会话列表，不直接进入单个聊天。
- 当前角色卡对应的 `{{char}}` 默认出现在好友列表；切换角色卡时随之变化。用户可以在当前聊天中临时删除，切换角色卡或新开酒馆聊天后恢复。
- 新增好友只能从全局通讯录选择；好友关系全局保存。群聊只能从已有好友中选择至少两人，创建时必须输入群名；群定义全局保存。
- QQ 消息按当前 SillyTavern 聊天存入 `chatMetadata.phonie_v2`，新开酒馆聊天后消息清空，但好友关系和群定义保留。
- 发送按钮具有两种明确状态：
  - 输入框有内容时发送一条 user 消息，并进入“等待角色回复”强调色；
  - 仍有输入内容时可继续发送多条；
  - 输入框为空且存在待回复消息时，再次点击才请求角色回复。
- 角色每批回复 1–8 条，按消息长度模拟打字延迟，使用约 `clamp(280 + 字符数×22ms, 320ms, 2600ms)` 的节奏依次出现。
- 支持文字、图片、转账、语音、表情包、引用和撤回；删除红包、音频下载等未要求功能。
- user 图片是描述式模拟图片卡；角色图片由 NovelAI 生成真实图片，每批最多一张。失败时保留描述、失败状态和重试入口，不影响同批其他消息。
- user 语音只保存文字描述；角色语音后台预生成 TTS、默认显示折叠波形，点击后播放并展开文字，不自动播放。
- 表情包全局通用。批量导入时按逗号拆项，再从每项第一个 `http://` 或 `https://` 位置拆分名称与 URL；支持预览、单删和清空。角色只能返回已导入的表情包名称。
- 转账仅是模拟消息卡，包含金额和备注，不引入余额系统。
- 单聊模型可返回主动来电意图；群聊不触发主动电话。主动来电按联系人独立冷却，默认 30 分钟、可配置为 0–1440 分钟。
- 主动来电先在灵动岛显示准备状态；完整脚本和前两段 TTS 准备成功后再切换到响铃页。当前正在生成、合成或通话时不得触发。

### 3. 电话

- 电话 APP 提供可输入拨号盘和通讯录多选。每位联系人按稳定 ID 生成不会指向真实号码的 `+00 xxx xxxx xxxx` 虚拟号码；输入精确匹配号码后拨打，未知号码直接提示不存在。
- user 外呼流程为拨号、准备脚本、接通，不出现接听/拒绝按钮；只有角色主动来电进入接听/拒绝页。
- 主动来电响铃 30 秒后自动记为未接；拒绝、超时、生成失败均写入当前聊天的普通通话记录。
- 通话内容可选择：
  - 延续当前酒馆实际上下文；
  - 自定义主题，选择后显示输入框。
- 上下文必须包含当前角色卡、Persona、当前聊天上下文窗口、世界书实际命中内容和对应 QQ 最近记录，不能只使用插件自己生成的摘要。
- 单人电话长度：短 4–6 段、普通 7–10 段、长 12–18 段；多人电话为 15–28 段。
- AI 不代写 user 台词：单人电话只生成远端角色发言；多人电话由所选联系人互相交谈。
- 脚本生成后先并发准备两段 TTS，再开始响铃或接通；剩余段落以并发 2 的队列继续生成。挂断时取消未完成请求。
- 单人来电页使用角色头像或首字母、来电原因、拒绝和接听按钮；接通后显示头像、通话时长、真实音频波形、字幕和挂断按钮。
- 非中文语音显示“小字原文＋大字译文”；中文原文只显示主字幕。译文默认简体中文，但可在设置中修改。
- 多人通话上方显示参与者姓名首字符圆圈；当前说话人高亮，字幕和波形随段落切换。
- 单段 TTS 失败时保留字幕和失败标记，经过自然停顿继续下一段；不会自动跨引擎。

### 4. 其他 APP

- 通讯录：
  - 全局保存当前角色、手动联系人及正文 `[TTS:…]` 标签中实际出现过的说话人；
  - 被删除的普通正文联系人进入忽略名单，后续再次说话也不会自动恢复，必须手动添加；
  - 每个联系人为六个引擎分别保存模型、音色、参考音频等覆盖项；
  - 下拉资源和手工 ID 使用可编辑组合框。
- 追踪：
  - 当前聊天普通记录可重播、重新渲染 TTS、收藏、删除；
  - 收藏时复制完整脚本和资源引用到全局收藏库，新开聊天后仍可播放；
  - 重播进入只读通话界面；重新渲染使用当前引擎及最新联系人绑定；
  - 删除资源时进行引用检查，收藏仍引用的音频不得误删。
- 引擎：
  - 只保留 IndexTTS2、GPT-SoVITS、VoxCPM2、Edge TTS、ElevenLabs、MiniMax，删除豆包 TTS；
  - 每个引擎使用独立 SVG 图标、代表色、列表卡和详情设置页；
  - 引擎详情包含配置、资源同步、连接检测、全局默认模型与音色；
  - 连接检测成功后才能设为当前引擎；失败时直接列明缺少的地址、密钥或服务组件；
  - IndexTTS2 只保留已支持的 JSON HTTP 接口，删除不可用的 Gradio 选项；
  - MiniMax 仅通过随项目提供的 SillyTavern 安全服务插件读取密钥、同步资源和合成，删除浏览器明文直连；
  - 联系人未绑定当前引擎时使用该引擎全局默认；两者均缺失才阻止合成；
  - 引擎失败不自动切换其他引擎。
- 绘画：
  - 每个命名预设保存前置固定正面词、后置固定正面词、固定负面词；
  - LLM 只生成动态正面 Tag，最终正面顺序固定为“前置＋动态＋后置”，负面只使用固定负面词；
  - 动态 Tag 可在绘制前手动修改；
  - 参数包括模型、横/竖/方尺寸、Sampler、Scheduler、Seed、Steps、Prompt Guidance、Guidance Rescale、Decrisper；
  - 尺寸固定为 `832×1216 / 1216×832 / 1024×1024`，单次只生成一张、无底图、Steps 最大 28，从接口层硬限制。该策略符合当前 NovelAI 的正常尺寸和 28 步说明，但 V5 仍可能受到 Opus 用量条限制。[NovelAI 订阅说明](https://docs.novelai.net/en/subscription/) [图像设置说明](https://docs.novelai.net/en/image/) [Steps 与 Guidance](https://docs.novelai.net/en/image/stepsguidance/)
  - 生成图片存入新版 IndexedDB，QQ 消息只保存资源键、描述、参数与 Seed。
- 主题：提供四模式选择、自定义颜色、壁纸、APP 图标预览、逐项重置和全部恢复默认。
- 设置：首页固定七个独立子页，每页左上角只返回设置首页：
  1. 模型来源；
  2. 显示与语言；
  3. 全部提示词；
  4. 正文 TTS；
  5. QQ 与主动来电；
  6. 表情包；
  7. 语音缓存。

### 5. 设置与提示词行为

- 模型来源支持跟随酒馆主 API 和多个自定义 OpenAI 兼容预设。
- 每个自定义预设包含名称、API 地址、独立 `secretId`、模型列表、当前模型、温度和最大 Token；温度默认 0.7，最大 Token 默认 8192、硬上限 30000。
- API Key 使用 SillyTavern 多密钥安全槽，不写入扩展设置。眼睛按钮可显示当前输入；保存后的密钥通过 `secretId` 调用酒馆揭示接口。若未开启 `allowKeysExposure`，保持掩码并给出准确配置提示。
- 删除自定义预设时经确认同步删除对应密钥 ID；删除当前预设后回到“跟随酒馆”，不静默选择其他自定义预设。
- 自定义 API 失败时不自动回退酒馆主 API，避免把上下文发送给非预期模型。
- 五种提示词工作流固定为：
  - 正文注入；
  - 单人电话；
  - 多人电话；
  - QQ 聊天；
  - 生图。
- 每种工作流支持多个命名预设和一个当前预设。每个条目独立保存名称、启用状态、角色、内容、插入深度和排序；角色只能为 `system / user / assistant`，深度为 0–20。
- 正文条目映射为独立 SillyTavern extension prompt 深度；QQ、电话和生图条目按各自深度插入实际生成消息列表，不能先压平成一个字符串。
- 公共变量提供中英文别名，至少包含：
  - `{{char}}`、`{{user}}`、`{{participants}}`；
  - `{{context}}`、`{{worldbook}}`、`{{storyHistory}}`；
  - `{{qqHistory}}`、`{{pendingMessages}}`；
  - `{{direction}}`、`{{callLength}}`；
  - `{{sourceLanguage}}`、`{{targetLanguage}}`；
  - `{{imageIntent}}`、`{{outputSchema}}`。
- 未知变量保留并在编辑器中标记，不能静默删除。
- 输出 JSON Schema 属于插件结构契约，独立于用户可编辑提示词，避免用户删除某条预设后破坏解析。
- 正文标签统一为：
  - `“{可见译文}”[TTS:{角色}:{情绪}:{原语言文本}]`
  - 解析器只把前三个冒号当作字段分隔，剩余内容属于文本；
  - 渲染后删除控制标签，保留可见译文并插入播放控件。
- 正文 TTS 子页包含三个独立开关：
  - 正文语音总开关；
  - 内置正文 TTS 提示词开关；
  - 自动渲染正文 TTS 开关。
- 自动渲染只在后台生成并缓存，不自动播放。提示词关闭时仍可解析手工生成的合法 `[TTS:…]` 标签；总开关关闭时不解析、不装饰、不生成。
- 音频焦点优先级固定为：电话 > QQ 语音 > 正文播放器。

## 三、公开数据与生成契约

### QQ 返回

```ts
type QQGenerationResult = {
  messages: Array<{
    kind: 'text' | 'voice' | 'image' | 'transfer' | 'sticker';
    speaker: string;
    sourceText?: string;
    translatedText?: string;
    emotion?: string;
    imageDescription?: string;
    amount?: number;
    note?: string;
    stickerName?: string;
  }>; // 1–8 条，每批最多一个 image
  proactiveCall?: {
    shouldCall: boolean;
    caller: string;
    reason: string;
    tone: string;
  };
};
```

### 单人电话返回

```ts
type SingleCallResult = {
  caller: string; // 来电为角色名，外呼为 user 名
  title: string;
  reason: string;
  tone: string;
  segments: Array<{
    speaker: string;
    sourceText: string;
    translatedText: string;
    emotion: string;
  }>;
};
```

`segments` 中不得生成 user 台词，段数按短、普通、长约束。

### 多人电话返回

```ts
type GroupCallResult = {
  sceneDescription: string;
  summary: string;
  speakers: string[];
  threads: string[];
  segments: Array<{
    speaker: string;
    sourceText: string;
    translatedText: string;
    emotion: string;
  }>;
};
```

`speaker` 必须来自所选联系人，`threads` 是简短话题名称数组，段数为 15–28。

### 生图返回

```ts
type ImagePromptResult = {
  dynamicPositiveTags: string;
};
```

### 存储边界

- `extension_settings.phonie_v2`：显示、语言、主题引用、模型预设、提示词、好友、群定义、通讯录、忽略名单、六引擎配置、联系人声线绑定、表情包、主动来电设置与冷却时间。
- `chatMetadata.phonie_v2`：当前酒馆聊天的 QQ 会话、待回复消息、普通通话记录、当前角色临时删除状态。
- `message.extra.phonie_v2`：正文 `[TTS:…]` 解析结果。
- IndexedDB `phonie-v2-assets`：音频、NovelAI 图片、上传壁纸、上传图标、全局收藏电话。
- SillyTavern Secrets：OpenAI、NovelAI、ElevenLabs、MiniMax 等密钥；设置中只保存密钥 ID。
- 旧 `phonie` 设置、聊天元数据、缓存和密钥不迁移、不删除。

## 四、测试与验收

- 保留当前 94 项测试作为重构前基线；删除与旧 10 APP、豆包、五项全局 Dock、`TTSVoice` 和玻璃终端设计绑定的旧断言，替换为 1.0 契约。
- 单元测试覆盖：
  - 五类提示词的逐条深度、角色、排序、变量与预设 CRUD；
  - QQ 1–8 条返回、单图限制、表情包名称校验、待回复按钮状态；
  - 单人和多人电话 Schema、段数、合法说话人及禁止 user 台词；
  - 30 分钟联系人独立冷却与单聊主动来电限制；
  - 全局好友/群/联系人与逐聊天消息/通话的存储隔离；
  - 固定角色临时删除、联系人忽略名单和全局收藏；
  - `[TTS:…]` 解析、可见译文保留和后台预生成；
  - 六引擎激活守卫、全局默认声线、无跨引擎回退；
  - NovelAI 四部分组合、单图、尺寸和 28 步硬限制；
  - 多 OpenAI 密钥 ID、揭示限制和删除行为。
- 集成测试覆盖酒馆主 API、自定义 OpenAI、世界书命中、聊天切换、角色卡切换、秘密槽、多媒体缓存和任务取消。
- 视觉验收尺寸至少包含桌面 `390×844`、Android `360×800`、`412×915`、短屏和横屏。
- 手工场景包括：
  - 发送多条 user 消息后一次请求 8 条角色回复；
  - 角色语音后台生成、点击播放；
  - 角色图片生成失败后单消息重试；
  - 主动来电准备、响铃、接听、拒绝和超时；
  - 28 段多人电话连续缓冲、说话人高亮和中途挂断；
  - 通话收藏跨聊天重播与重新渲染；
  - 缺少 MiniMax 服务插件、无声线绑定、密钥不可揭示等失败页；
  - 三个内置主题与自定义上传素材；
  - Battery API、网络类型或音频自动播放能力缺失时的诚实降级。
- 最终必须通过 `npm test`、`npm run check`，并在 Android Chrome/WebView 与桌面 Chromium 完成视觉和真实音频回归。
- UI 文案只写直接的状态、原因和下一步，不加入玩笑、无关括号、虚构限制或类似“（无东坡肉）”的内容。

## 五、锁定假设与用户原始需求

- 主要目标平台是 Android Chrome/WebView 和桌面 Chromium；iOS Safari 仅保证基本可用。
- UI 完整交付简体中文；译文语言默认简体中文但允许修改。
- 移动端“全屏”指占满 SillyTavern WebView 的可用视口并保留手机外壳，不主动调用浏览器原生 Fullscreen API。
- 实施顺序固定为：新版命名空间与模块边界 → 手机外壳和导航 → 设置/主题/通讯录/引擎 → QQ → 电话/追踪 → 绘图与正文 TTS → 全量 QA 与文档。

用户原始需求如下，作为产品范围原文保留，不由实施者重新解释或删改：

```text
重做吧，整个界面，都重做吧，受不了这些错误了。 接下来我将和你讨论好整个手机内容，之后需大幅重做。

# phonie手机——兼具TTS/聊天/绘图的SillyTavern小手机插件

1. 我需要真实的手机体验，所有UI必须像是一个真实的手机，有模拟真实手机外壳、占据真实手机屏幕的全屏、能显示生成中/通话中动效的灵动岛、顶部真实时间电量网络状态栏、底部浮动底栏，所有图标不允许用emoji代替。手机主题有三个，为日间/夜间/跟随酒馆主题。各个主题有各自的主题色60%/重点色30%/点缀色7%/微妙的渐变色3%。
2. 第一页主页的APP依次排列过去为：QQ，电话，通讯录，追踪，引擎，绘画，主题，设置。
3. 各个APP的功能：

- QQ：模拟真实QQ，里面有固定的一位好友`{{char}}`，此好友随着角色卡的改变而改变，聊天记录保存在当前聊天存档里，新开聊天会重置聊天记录。此外可以加好友，所加的好友必须是通讯录里有的；还有创建群聊，点击创建群聊后，将从已有好友列表里自行选择多位好友创建。角色的聊天记忆用变量来读取酒馆**实际**上下文，包括世界书，此外电话功能所使用的记忆来源也相同。聊天可模拟发送图片（文字描述版）、转账、语音（其中user发送的语音为文字描述，char发送的语音为tts，默认语音收起为音波条，点击后出现文字，char的语音条点击后出现tts语音）、表情包（批量导入表情包的方式为：表情包名字URL,表情包名字URL,表情包名字URL,）。user发信息后发送按钮变色，再次点击发送按钮则char回复，没有点击按钮而是继续输入文字的话可以输入多条消息。char回复消息可回复多条，每一条消息模拟真实打字速度，短消息弹出快，长消息弹出慢。char也可发送图片、转账、语音、表情包，其中char发送的表情包必须从导入的表情包名字里选择，表情包全局通用。右上角有聊天设置，可以选择清空聊天或者删除好友（聊天预设放在设置APP处）。聊天时llm可以分析此时的情况要不要打来主动电话（详见电话APP）。

- 电话：tts通话，包括单人电话和多人聊天，通话人物从通讯录的已有角色里选择，通话内容可以拉选框选择从酒馆上下文延续或自定义聊天主题，点击自定义聊天主题后弹出输入框让user输入想要聊的主题，随后llm生成聊天内容后由tts引擎生成对话。注意，这里的电话只是模拟电话，因为user输入一句再等llm返回tts生成的话会非常慢，所以由ai一次性模拟整个对话。可以选择短来电 · 4–6 句，普通 · 7–10 句，长来电 · 12–18 句；多人聊天对话长度看人数需要达到15-28句。这是user打过去的电话，而由char打过来的主动电话在聊天处触发（详见聊天APP）。电话界面模拟真实手机拨号界面，可以自由输入数字，由插件生成char和正文说话人物的虚拟电话号码供user输入或拉选多人对话，拨打电话后进入全屏通话界面。单人通话界面中，会出现接听和拒绝按钮，此时角色头像在中间偏上，如图（p1）所示：

接通后，角色头像或首字母/文字放在界面较上方，最下方有挂断按钮，中下段放tts文字内容（如果是除了中文之外的语言，还要小字放对应的外文和大字中文译文）如图（p2）所示（图中的字号大小要反过来，大字是中文，小字是外文），此外还要有音波动效和通话中的秒数。

多人通话时在通话界面较上方放上各个角色的名字首位圆圈图，轮到谁说话，谁的头像就亮起来。 单人对话需要返回：caller:,title:,reason:,tone:,segments:。 多人对话需要返回：sceneDescription:,summary:,speakers:,threads:,segments:。

- 通讯录：记录正文里有说过话的角色，可以添加和删除角色，此通讯录全局通用。通讯录还有一个作用是记录角色的音色ID/语音模型并绑定，各个引擎的音色都可以选择绑定，音色可拉选和自己填写。

- 追踪：放置单人/多人通话的通话记录，可以重复播放或者重新渲染tts，可收藏和删除。

- 引擎：有IndexTTS2，GPT-SoVITS，VoxCPM2，Edge TTS，ElevenLabs，MiniMax。每个引擎有不同的图标和对应的代表色，每个引擎点进去都是单独对应的设置界面，点击左上角返回可返回引擎选择界面。

- 绘画：文生图功能，连接novelai。基本设置需要固定正面提示词，后置固定正面提示词，固定负面提示词，以上三个提示词设置需要能保存为预设并能够删除新增等。随后是能变动的生图提示词，此部分提示词交给llm判断，由llm生成此部分变动生图提示词后，再把这四部分提示词tag交给NAI进行绘制。novelai的各个调整参数也要有，横图/竖图/方图，28步数等，均不能超过会员免费生小图的上限。此绘图的作用是聊天APP使用的，char和user聊天时，分享自己想要给user看得图片时，就使用llm生成对应的tag来生图。

- 主题：日间/夜间/跟随酒馆主题/自定义，其中，自定义可定义APP图标和手机背景。

- 设置：设置界面存放所有重要的设置功能，每个功能点进去后单独跳出一页，每一页有返回按键，点击左上角的返回按钮会回到设置界面。设置界面第一项是模型来源，可以选择跟随酒馆主API，或者自定义兼容openai。自定义openai需要可保存可删除可新增预设，自定义API地址，填写API key（有小眼睛按钮可以隐藏和显现），可拉取的模型列表，温度（默认0.7），最大token（最多30000）。 第二项是显示，入口为悬浮球/酒馆魔棒菜单/两者都显示。第三项是全部提示词预设，记录注入正文的提示词，单人来电提示词，多人来电提示词，聊天提示词，生图提示词这五中，每种都可以自定义编辑预设，预设条目名字，插入深度，角色是系统/用户/助手，条目位置调整，增加/删除条目。需要有可供使用的变量。其中注入正文的语音标签的格式为：“{译文}”[TTS:{角色}:{情绪}:{文本}]，由插件渲染后面一段`[]`的内容，保持“{译文}”[TTSVoice:{角色}:{情绪}:{文本}]译文可见。第三项是最语音聊天开关、内置正文TTS提示词开关、自动渲染正文TTS语音开关。后一项是语音缓存，可显示有多少段，占了多少MB，删除缓存按钮。

注意，我整个插件都不需要诸如（无东坡肉）这一类的提示。详见图3。你看看有什么需要补充的细节或者需要修改的地方，和我讨论完后，把我的原话写进计划里。
```
